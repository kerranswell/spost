<?php

class posts extends record {

/*    public function GetItem($key, $fields = '*') {

        $sql = "SELECT p.".$fields.", i.name as bg_image_name FROM `pages` p
                left join `images` i on (i.idx = p.bg_image)
                WHERE p.`id` = ?";
        $item = $this->dsp->db->SelectRow($sql, $key);

        return $item;
    } // GetItem()*/


}

?>